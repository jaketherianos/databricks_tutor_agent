# Small Tutor Agent - Streamlit Web App & MLflow Scorers
# Requirements for running locally and on Databricks Apps

# Core dependencies
streamlit>=1.28.0
mlflow[databricks]>=2.11.0
pandas>=2.0.0

# Agent dependencies
python-dotenv>=1.0.0
databricks-sdk>=0.18.0

# For LLM judges (optional - only needed if using built-in/custom LLM scorers)
openai>=1.0.0

# Optional but recommended for production
numpy>=1.24.0

